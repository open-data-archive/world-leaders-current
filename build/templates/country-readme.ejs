# <%= config.name_local !== config.name_en ? config.name_local + ' / ' : '' %><%= config.name_en %>

> <% if (config.local_lang !== 'en') { %>ผู้นำปัจจุบันของ<%= config.name_local %> / <% } %>Current leaders of <%= config.name_en %>
>
> Last updated: <%= lastUpdated %>

<% const flag = flags[config.iso_code] || '' %>
## <%= flag %> <% if (config.local_lang !== 'en') { %>ผู้นำปัจจุบัน / <% } %>Current Leaders

<% for (const [posKey, pos] of Object.entries(data.positions)) { %>
<% if (!pos.current_holder) continue %>
<% const isRef = pos.is_same_as_head_of_state && posKey === 'head_of_state' %>
<% if (isRef) continue %>
### <%= pos.title_local && pos.title_local !== pos.title_en ? pos.title_local + ' / ' : '' %><%= pos.title_en %><% if (pos.is_same_as_head_of_state) { %> (Head of State and Government)<% } %>
<% if (pos.is_same_as_head_of_state) { %>

In <%= config.name_en %>, the <%= pos.title_en %> serves as both head of state and head of government.
<% } %>

| | |
|--|--|
<% if (pos.current_holder.name_local) { %>| **<%= config.local_lang !== 'en' ? 'ชื่อ / ' : '' %>Name** | <%= pos.current_holder.name_local %> / <%= pos.current_holder.name_en %> |
<% } else { %>| **Name** | <%= pos.current_holder.name_en %> |
<% } %><% if (pos.current_holder.regnal_name_en) { %>| **Regnal Name** | <%= pos.current_holder.regnal_name_en %> |
<% } %><% if (pos.current_holder.since) { %>| **<%= pos.is_monarchy ? 'Reign Since' : 'Since' %>** | <%= pos.current_holder.since %> |
<% } %><% if (pos.current_holder.party) { %><% if (pos.current_holder.party_local && pos.current_holder.party_local !== pos.current_holder.party) { %>| **Party** | <%= pos.current_holder.party_local %> / <%= pos.current_holder.party %> |
<% } else { %>| **Party** | <%= pos.current_holder.party %> |
<% } %><% } %><% if (pos.current_holder.born) { %>| **Born** | <%= pos.current_holder.born %> |
<% } %>

<% if (pos.current_holder.wikipedia_en || pos.current_holder.wikipedia_local) { %>
Read more:
<% if (pos.current_holder.wikipedia_local) { %>- [<%= localWikiLabel %>](<%= pos.current_holder.wikipedia_local %>)
<% } %><% if (pos.current_holder.wikipedia_en) { %>- [Wikipedia English](<%= pos.current_holder.wikipedia_en %>)
<% } %>
<% } %>
<% } %>

<% if (changes && changes.length > 0) { %>
## Recent Changes

<% for (const change of changes.slice(0, 5)) { %>
**<%= change.change_date || change.detected_at?.split('T')[0] %>** — <%= change.new_holder_en %> became <%= change.position_title || change.position %>

Predecessor: <%= change.old_holder_en %>

<% } %>
<% } %>

<% if (aiContent) { %>
## Government System

<%= aiContent %>

<% } %>

## Data

- [current.json](./current.json) — Current leaders (JSON)
- [history.json](./history.json) — Historical leaders
- [changes.json](./changes.json) — Change log

## Links

- [Wikipedia: <%= config.name_en %>](https://en.wikipedia.org/wiki/<%= encodeURIComponent(config.name_en.replace(/ /g, '_')) %>)
<% if (data.positions.head_of_government?.office_wikipedia_en) { %>- [<%= data.positions.head_of_government.title_en %> of <%= config.name_en %>](<%= data.positions.head_of_government.office_wikipedia_en %>)
<% } %><% if (data.positions.head_of_state?.office_wikipedia_en && !data.positions.head_of_state?.is_same_as_head_of_state) { %>- [<%= data.positions.head_of_state.title_en %> of <%= config.name_en %>](<%= data.positions.head_of_state.office_wikipedia_en %>)
<% } %>

## Disclaimer

Data on this page is sourced from Wikidata and Wikipedia via their free APIs under CC-BY-SA-4.0 license.
For most current information, refer to Wikipedia directly.

## Related

<% if (config.region.includes('asean')) { %>- [ASEAN region](../../by-region/asean/)
<% } %><% if (config.region.includes('g20')) { %>- [G20](../../by-region/g20/)
<% } %>- [All countries](../)
- [All changes](../../changes/)
